"""Builder for Category 12: 大厂原题 (stub)

All problems in this category are stubs with empty tables=[].
This builder exists so generate_data.py doesn't fail on import,
but no tables are created.
"""

import sqlite3


def build(conn: sqlite3.Connection):
    """No tables to create — all problems are stubs."""
    print("     (stub: no tables)")
